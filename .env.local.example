# Copy this file to .env.local for local development,
# and add NEXT_PUBLIC_API_URL as an Environment Variable in Vercel for production.

# Must point at your Render backend's /api base path (include https:// and /api).
NEXT_PUBLIC_API_URL=https://rayvice-backend.onrender.com/api
